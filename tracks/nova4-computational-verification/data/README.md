# Nova 4 Data

The committed summary is an exact finite audit of all targets through `n=13`. The detailed `lambda` arrays are regenerated deterministically and identified by SHA-256.

No finite dataset is an asymptotic theorem.
