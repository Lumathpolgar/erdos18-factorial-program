#include <iostream>

int main() {
    std::cerr
        << "FAIL: marker_three_mitm_prefix_u128.cpp is retired. "
        << "Unrestricted unsigned-128 half-list multiplication is disproved as an exact verifier at n=57. "
        << "Build and use marker_three_mitm_checkpoint_u128.cpp instead.\n";
    return 1;
}
