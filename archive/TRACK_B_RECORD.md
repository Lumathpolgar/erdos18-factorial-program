# Conditional Track B Record

## Package

`Erdos18_Solution_Program_Phases1-8_13_14A_14B1_14B2_14C_14D_14E_15.zip`

## Scope

Track B develops the conditional local-to-global and factorial conversion machinery. Its late phases include:

- Phase 13: factorial reduction and half-range target
- Phase 14A: packet architecture
- Phase 14B1 and 14B2: packet refinements and exact audits
- Phase 14C: scaled core and descent structure
- Phase 14D: boundary and entropy analysis
- Phase 14E: dense quotient and high-zone descent analysis
- Phase 15: clean-room audit and conditional manuscript

## Conditional conclusion

Track B supports the implication

```text
suitable local half-range theorem
    =>
h(n!) = O((log n)^3).
```

The exact local hypothesis is recorded in the package merge contract. In the historical lcm route it was supplied by the unproved theorem

```text
H_{L_m}(floor(L_m^(1/3)) + 1) = O(log m).
```

## Phase 15 status

Phase 15 reconstructs the conditional argument independently and intentionally keeps the merge gate closed.

The package must not be described as an unconditional solution. Its value is that the endgame need not be rebuilt once an adequate direct factorial or lcm-core theorem is proved.

## Direct factorial reuse

The direct factorial program should audit whether the Track B half-range conversion can accept the theorem

```text
H_{n!}(floor(sqrt(n!)) + 1) = O((log n)^2)
```

directly. Any adapted implication must receive a new theorem contract rather than silently modifying the historical Track B claim.
